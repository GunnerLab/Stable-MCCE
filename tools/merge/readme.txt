merge_pdb merges multiple step2_out.pdb into one and removes the duplicated ones.

Usage: merge_pdb -i input_list -o output_pdb -p param_dir

input_list:   a text file includes a list of pdb file you want to merge
output_pdb:   output pdb file
param_dir:    parameter directory

